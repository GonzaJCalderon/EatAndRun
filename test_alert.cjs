const fs = require('fs'); const path = require('path'); function walk(dir) { const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) walk(file); else if (file.endsWith('.jsx')) { const content = fs.readFileSync(file, 'utf8'); if (content.includes('<Alert')) { const singleLine = content.replace(/\n/g, ' '); if (!singleLine.match(/import\s*{[^}]*Alert[^}]*}\s*from\s*['\x22]@mui\/material['\x22]/)) { console.log('MISSING IMPORT:', file); } } } }); } walk('./src');
